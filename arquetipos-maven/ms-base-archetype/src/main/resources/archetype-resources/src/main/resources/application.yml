server:
  port: \${serverPort:8080}

spring:
  application:
    name: \${artifactId}

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    preferIpAddress: true
